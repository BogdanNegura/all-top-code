import styled from "styled-components";

export const StyledLogoWrapper = styled.div`
  font-size: 1.2rem;
  color: var(--color-white);
  font-weight: 400;
  line-height: 50px;

  & > img {
    height: 80px;
    width: 80px;
  }
`;
